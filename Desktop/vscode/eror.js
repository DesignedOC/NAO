// string / number / boolean  (nom pt contenir des chiffres sauf 1re lettre)
// + - / *
// +=    -=
// alert()
// prompt()                                                    - input
// parseInt()                                                  - ("2") + ("2") = 22  but w parseInt -> 4
// toString()
// typeof()
// concaténation
// function newFonction()            alreadyCreated()
// confirm() -> retourne un booléen
// opérateurs (< > == === !== != &&)                           - renvoie un booléen
// ||                                                          - rassemble les var et retourne celle qui est true
// if    else if    else                                       - se sert du résultat du booléen pr influer sur le code
// switch (variableAanalyser) case : break default:            - peut mettre qu'1 valeur, pas pas contenir d'opérateurs
// while                                                       - Pour faire fonctionner une boucle, il est nécessaire de définir une condition. Tant que celle-ci est vraie (true), la boucle se répète.
// document.createElement()
// document.getElementById()
// addEventListener("click", nomFonction())                   - remove () from nomFonction , sinn la fonction s'éxécute sans qu'on clique
// appendChild()
// split(" ")                                                 - transforme une var en tableau, à chaque espace -> prend la donnée et en fait une valeur du tab
// join()
// push()
// for in
// isNaN                                                      - is the type of this var a number ? return true if it's not a number
